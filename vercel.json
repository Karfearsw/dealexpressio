{
  "version": 2,
  "buildCommand": "cd server && npm install && npm run build && npm run migrate:prod && cd ../client && npm install && cd .. && npm run client:build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}